# homebrew-tap

Homebrew tap for [polius](https://github.com/polius) tools.

## Install

```bash
brew install polius/tap/fsend
```

`fsend` — peer-to-peer, end-to-end encrypted file transfer ([repo](https://github.com/polius/fsend)).
